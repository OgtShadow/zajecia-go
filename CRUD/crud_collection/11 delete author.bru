meta {
  name: delete author
  type: http
  seq: 11
}

delete {
  url: http://localhost:8080/autorzy/1
  body: none
  auth: none
}

tests {
  test("author deleted message", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.message).to.equal("author deleted");
  });
}
