meta {
  name: get track by id
  type: http
  seq: 8
}

get {
  url: http://localhost:8080/utwory/1
  body: none
  auth: none
}

tests {
  test("track with id 1 has id field", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.id).to.equal(1);
  });
}
