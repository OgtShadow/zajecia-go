meta {
  name: invalid nip
  type: http
  seq: 7
}

post {
  url: http://localhost:8080/validate?nip=52526581151
  body: none
  auth: none
}

params:query {
  nip: 52526581151
}

tests {
  test("validate expect false", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.is_valid).to.equal(false);
  });
}
