meta {
  name: get req
  type: http
  seq: 7
}

get {
  url: http://localhost:8080/validate?nip=52526581151
  body: none
  auth: inherit
}

params:query {
  nip: 52526581151
}

tests {
  test("valid error message response for GET", async function () {
    expect(res.status).to.equal(405);
  
    const msg = (res.getBody() || "").trim();
    expect(msg).to.equal("only POST method is supported");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
