meta {
  name: invalid pesel
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/validate?pesel=440514014583
  body: none
  auth: none
}

params:query {
  pesel: 440514014583
}

tests {
  test("validate expect false", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.is_valid).to.equal(false);
  });
}
