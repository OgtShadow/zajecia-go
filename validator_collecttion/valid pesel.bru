meta {
  name: valid pesel
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/validate?pesel=44051401458
  body: none
  auth: none
}

params:query {
  pesel: 44051401458
}

tests {
  test("validate expect true", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.is_valid).to.equal(true);
  });
}
