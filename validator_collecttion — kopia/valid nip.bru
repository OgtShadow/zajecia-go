meta {
  name: valid nip
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/validate?nip=5252658115
  body: none
  auth: none
}

params:query {
  nip: 5252658115
}

tests {
  test("validate expect true", async function () {
    const responseJson = res.getBody ? res.getBody() : await res.json();
    expect(responseJson.is_valid).to.equal(true);
  });
}
